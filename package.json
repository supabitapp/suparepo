{
  "name": "suparepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "supaimg.desktop:dev": "cd apps/supaimg-desktop && bun run tauri -- dev",
    "supaimg.desktop:build": "cd apps/supaimg-desktop && bun run tauri -- build",
    "supaimg.app:dev": "bun run --filter supaimg.app dev",
    "supaimg.app:build": "bun run --filter supaimg.app build",
    "supabit.app:dev": "bun run --filter supabit.app dev",
    "workflows:gen": "bun scripts/generate-workflows.ts",
    "workflows:check": "bun scripts/generate-workflows.ts --check",
    "lint": "bun run workflows:check && oxlint .",
    "lint:fix": "oxlint --fix .",
    "fmt": "oxfmt .",
    "fmt:check": "oxfmt --check .",
    "check-types": "turbo run check-types",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "oxfmt": "^0.24.0",
    "oxlint": "^1.39.0",
    "prettier": "^3.7.4",
    "turbo": "^2.7.5",
    "typescript": "5.9.2"
  },
  "lint-staged": {
    "*": "oxfmt --no-error-on-unmatched-pattern",
    "*.{js,jsx,ts,tsx,mjs,cjs}": "bun run lint",
    "apps/supaimg-desktop/src-tauri/**/*.rs": [
      "bash -c 'cargo fmt --manifest-path apps/supaimg-desktop/src-tauri/Cargo.toml --all -- --check'",
      "bash -c 'cargo clippy --manifest-path apps/supaimg-desktop/src-tauri/Cargo.toml --all -- -D warnings'"
    ]
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "bun@1.3.2"
}
